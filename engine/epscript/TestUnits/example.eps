function fibonacci(n) {
    function closure(m) {
        if (m < 2) {
            return 1
        }
        return closure(m - 1) + closure(m - 2)
    }
    var res = []
    for i from 0 to n {
        res.append(closure(i))
    }
    return res
}

output(fibonacci(Integer(readline("输入长度："))))